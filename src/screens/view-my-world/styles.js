import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    // borderTopWidth: 40,
    // borderColor: "black",
    backgroundColor: "#fff",
    flex: 1,
  },
  preview: {
    flex: 1,  
  },
})

export default styles 